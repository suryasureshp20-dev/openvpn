name: Sync GitHub → GitLab (development → development)

on:
  push:
    branches:
      - development

jobs:
  gitlab-sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout GitHub code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Full clone required for pushing to new branches

      - name: Push to GitLab (development → development)
        run: |
          git config --global user.name "Surya Suresh"
          git config --global user.email "suryasuresh.p20@gmail.com"
          git remote add gitlab https://oauth2:${GITLAB_TOKEN}@gitlab.com/automationrealm/arrms.git
          
          # Push GitHub 'development' to GitLab 'development'
          git push gitlab development:development  # DO NOT use --force
        env:
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
